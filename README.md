# Lab 02: Python Functions

## Task
Complete the function `calculate_area` in `main.py`.
It should take `length` and `width` as arguments and return the area.

## Example
calculate_area(5, 10) should return 50.

## Grading
This lab is automatically graded. Push your code to run the tests.